# 🏥 医院病历管理系统

Hospital Medical Record Management System

## 📋 项目简介

这是一个功能完整的医院病历管理系统，采用前后端分离架构，支持患者管理、医生管理、病历管理、处方管理、统计分析等功能。

### 技术栈

**后端**:
- Node.js 20.x + Express.js 4.x
- TypeScript 5.x (严格模式)
- Prisma 5.x ORM + MySQL 8.x
- JWT 认证 + bcrypt 加密

**前端**:
- Vue 3.x (Composition API)
- Vite 5.x + TypeScript 5.x
- Element Plus 2.x + TailwindCSS 3.x
- Pinia 2.x + ECharts 5.x

---

## 🚀 快速开始

### 前置要求

- Node.js 20.x LTS
- MySQL 8.x
- npm 10.x

### 1. 数据库设置

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE hospital_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
```

### 2. 后端设置

```bash
cd backend

# 安装依赖
npm install

# 配置环境变量 (编辑 .env 文件)
# DATABASE_URL="mysql://root:your_password@localhost:3306/hospital_db"

# 生成 Prisma 客户端
npx prisma generate

# 运行数据库迁移
npx prisma migrate dev --name init

# 启动后端服务
npm run dev
```

后端将在 **http://localhost:3000** 启动

### 3. 前端设置

```bash
cd frontend

# 安装依赖
npm install

# 启动前端服务
npm run dev
```

前端将在 **http://localhost:5173** 启动

---

## 📦 导入测试数据

系统提供了完整的测试数据，包含用户、科室、医生、患者、病历、处方等信息。

### 方式一：使用 SQL 文件导入（推荐）

```bash
# 确保数据库已创建并完成迁移后
cd backend

# 使用 MySQL 命令行导入
mysql -u root -p hospital_db < prisma/test_data.sql
```

或在 MySQL 客户端中执行：

```sql
USE hospital_db;
SOURCE E:/Projects/XRY/hospital-medical-system/backend/prisma/test_data.sql;
```

### 方式二：使用种子脚本

```bash
cd backend
npm run prisma:seed
```

> **注意**: 种子脚本只包含基础的4个用户，如需完整测试数据请使用方式一。

### 测试数据内容

| 数据类型 | 数量 | 说明 |
|---------|------|------|
| 用户账户 | 22 | 管理员、医生、护士、前台 |
| 科室 | 13 | 内科、外科、儿科等 |
| 医生档案 | 10 | 关联用户和科室 |
| 患者记录 | 30 | 完整的患者信息 |
| 病历 | 50 | 包含诊断和治疗方案 |
| 处方 | 76 | 药品和用法用量 |
| 操作日志 | 30 | 系统操作记录 |

---

## 👤 测试账户

导入测试数据后，可使用以下账户登录：

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | admin123 | 系统管理员 |
| 管理员 | superadmin | superadmin123 | 超级管理员 |
| 医生 | doctor1 | doctor1123 | 内科医生 |
| 医生 | doctor2 | doctor2123 | 外科医生 |
| 医生 | doctor3 ~ doctor10 | doctor3123 ~ doctor10123 | 其他科室医生 |
| 护士 | nurse1 | nurse1123 | 护士 |
| 护士 | nurse2 ~ nurse5 | nurse2123 ~ nurse5123 | 其他护士 |
| 前台 | reception1 | reception1123 | 前台接待 |
| 前台 | reception2 ~ reception4 | reception2123 ~ reception4123 | 其他前台 |

> **密码规则**: 所有测试账户密码格式为 `用户名 + 123`

---

## 📁 项目结构

```
hospital-medical-system/
├── backend/                    # 后端 API
│   ├── prisma/
│   │   ├── schema.prisma      # 数据库模式
│   │   ├── seed.ts            # 种子数据脚本
│   │   ├── test_data.sql      # 完整测试数据
│   │   └── migrations/        # 数据库迁移
│   ├── src/
│   │   ├── controllers/       # 控制器
│   │   ├── services/          # 业务逻辑
│   │   ├── routes/            # API 路由
│   │   ├── middlewares/       # 中间件
│   │   └── validators/        # 验证器
│   └── uploads/               # 上传文件
├── frontend/                   # 前端应用
│   └── src/
│       ├── views/             # 页面组件
│       ├── components/        # 公共组件
│       ├── stores/            # 状态管理
│       └── api/               # API 服务
└── docs/                       # 项目文档
```

---

## 📊 功能模块

- ✅ **用户认证**: JWT 令牌、角色权限控制
- ✅ **患者管理**: 患者信息 CRUD、病历号自动生成
- ✅ **医生管理**: 医生档案、科室分配
- ✅ **病历管理**: 病历记录、状态工作流、处方管理
- ✅ **附件管理**: 文件上传下载 (PDF/JPG/PNG)
- ✅ **统计分析**: 仪表板、图表可视化、报表生成

---

## 📚 详细文档

详细的项目文档位于 `docs/` 目录：

| 文档 | 说明 |
|------|------|
| [SETUP.md](./docs/SETUP.md) | 完整的设置和部署指南 |
| [PROJECT_STATUS.md](./docs/PROJECT_STATUS.md) | 项目状态和进度 |
| [PHASE1_SUMMARY.md](./docs/PHASE1_SUMMARY.md) | 第一阶段：基础架构 |
| [PHASE2_SUMMARY.md](./docs/PHASE2_SUMMARY.md) | 第二阶段：核心功能 |
| [PHASE3_SUMMARY.md](./docs/PHASE3_SUMMARY.md) | 第三阶段：统计分析 |

> 所有文档均提供中文版本 (*.zh-CN.md)

---

## 🔧 常用命令

### 后端

```bash
npm run dev          # 启动开发服务器
npm run build        # 编译 TypeScript
npm run prisma:seed  # 运行种子脚本
npm run prisma:studio # 打开数据库管理界面
```

### 前端

```bash
npm run dev          # 启动开发服务器
npm run build        # 构建生产版本
npm run preview      # 预览生产版本
```

---

## 📝 API 端点

- **认证**: `/api/v1/auth/*` (登录、登出、刷新令牌)
- **患者**: `/api/v1/patients/*` (CRUD、病史)
- **医生**: `/api/v1/doctors/*` (CRUD、统计)
- **科室**: `/api/v1/departments/*` (查询)
- **病历**: `/api/v1/medical-records/*` (CRUD、状态流转)
- **处方**: `/api/v1/prescriptions/*` (CRUD)
- **附件**: `/api/v1/attachments/*` (上传、下载)
- **统计**: `/api/v1/statistics/*` (仪表板、报表)

---

## 📄 许可证

本项目用于学习和演示目的。

---

*最后更新: 2026-02-05*
